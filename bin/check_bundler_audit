#!/usr/bin/env ruby

require_relative '../environment'

module Garrison
  module Checks
    check = CheckBundlerAudit.new(@options)
    check.perform
    Api::Alert.obsolete_previous_runs(check.source) if ENV["GARRISON_AUTO_OBSOLETE"]
  end
end
